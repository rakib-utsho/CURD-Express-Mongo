<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Chat</title>
</head>
<body>
    <p>Edit Your Chat: <%= chat._id%></p>
    <p>This was sent from <strong><%= chat.from %></strong> to <strong><%= chat.to %></strong></p>

    <form method="POST" action="">
        <textarea name="message" rows="10" cols="40"><%= chat.message%></textarea>
        <br>
        <button>Edit</button>
    </form>
</body>
</html>